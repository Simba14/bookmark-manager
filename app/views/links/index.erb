<h2> List of Links </h2>
<ul id='links'>
  <%= p @links%>
  <% @links.each do |link| %>
    <li>
      Title: <%= link.title %>
      URL: <%= link.url %>
      Tag: <% link.tags.each do |tag| %>
        <%=tag.name%>
      <%end%>
    </li>
  <% end %>
</ul>
